# Exemplo de variáveis de ambiente.
# Copie este arquivo para `.env.local` e preencha conforme necessário.
#
# A versão atual roda 100% com estado em memória e NÃO requer nenhuma variável.
# As entradas abaixo são placeholders para quando um backend de persistência
# (ex.: Supabase) for conectado.

# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
